# Резюме: Удаление поля 'name' из frontmatter

## ✅ Все задачи выполнены

### Внесенные изменения:

1. **src/domain/types.ts** ✅
   - Удалено поле `name?: string;` из интерфейса `TrackerFileOptions`

2. **src/services/tracker-file-service.ts** ✅
   - Удалено чтение поля `name` из frontmatter (строки 180-182)
   - Удалена запись поля `name` при создании файла (строка 25)

3. **src/core/tracker-plugin.ts** ✅
   - Заменено `const baseName = (fileOpts.name?.trim() || file.basename);`
   - На `const baseName = file.basename;`

4. **src/ui/modals/create-tracker-modal.ts** ✅
   - Удалена строка `const escapedName = name.replace(/"/g, '\\"');`
   - Изменено `let newFrontmatter = \`name: "${escapedName}"\\ntype: "${type}"\\n\`;`
   - На `let newFrontmatter = \`type: "${type}"\\n\`;`

5. **src/ui/modals/edit-tracker-modal.ts** ✅
   - Удалена строка `const escapedName = name.replace(/"/g, '\\"');`
   - Изменено `let newFrontmatter = \`name: "${escapedName}"\\ntype: "${type}"\\n\`;`
   - На `let newFrontmatter = \`type: "${type}"\\n\`;`

6. **README.md** ✅
   - Удалено поле `name: Название трекера` из примера структуры файла
   - Добавлено пояснение: **"Имя трекера определяется именем файла (без расширения .md)"**

## 📊 Результат

### Было:
```yaml
---
name: "Утренняя зарядка"
type: "good-habit"
data: {}
---
```
Файл: `Утренняя зарядка.md`

### Стало:
```yaml
---
type: "good-habit"
data: {}
---
```
Файл: `Утренняя зарядка.md`

**Имя трекера = имя файла** (`file.basename`)

## ✅ Проверки

- ✅ Код компилируется без ошибок: `npm run build` - **SUCCESS**
- ✅ Размер bundle: 591.7kb (было 592.1kb) - небольшое уменьшение
- ✅ Нет упоминаний `fileOpts.name` в коде
- ✅ Нет чтения/записи поля `name:` в frontmatter
- ✅ README обновлен

## 🎯 Преимущества

1. **Упрощение структуры данных**
   - Меньше полей в frontmatter
   - Меньше кода для поддержки

2. **Автоматическая синхронизация**
   - Имя трекера всегда соответствует имени файла
   - При переименовании файла автоматически меняется имя трекера

3. **Меньше возможностей для ошибок**
   - Невозможно рассинхронизировать имя файла и name в frontmatter
   - Проще логика обработки

4. **Понятнее для пользователя**
   - Что видишь в файловом менеджере = имя трекера
   - Не нужно открывать файл чтобы узнать "настоящее" имя

## 📝 Что нужно сделать пользователю

Пользователь сам удалит поле `name` из существующих файлов трекеров. 

Плагин будет работать корректно:
- Новые трекеры создаются без поля `name`
- Существующие трекеры с полем `name` будут игнорировать это поле
- Везде используется `file.basename` как имя трекера

## 🔄 Миграция (опционально)

Если пользователь хочет почистить старые файлы, можно выполнить поиск и замену в frontmatter:

**Найти:**
```regex
^name:.*\n
```

**Заменить на:** (пустую строку)

Это удалит все строки с `name:` из frontmatter во всех файлах.

---

**Все изменения готовы и протестированы! ✅**

